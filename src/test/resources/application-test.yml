spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver  # Certifique-se de que este é o driver configurado
    username: sa
    password: ""
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    url: jdbc:h2:mem:testdb
    enabled: true
    locations:
      - classpath:db/migration/test
    user: sa
    password: ""



